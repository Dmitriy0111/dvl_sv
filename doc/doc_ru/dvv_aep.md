# dvv_aep (analysis export)

Данный файл содержит описание класса порта, необходимого для одностороннего обмена между верификационными компонентами совместно с классом dvv_ap (analysis port). Данный класс является приёмником, а предметом обмена может служить системный или пользовательский тип.

## Поля и функции/задачи класса  

Класс содержит следующие поля:
| Имя       | Тип                 | Описание                    |
| --------- | ------------------- | --------------------------- |
| p_name    | string              | Имя порта                   |
| bp_list   | dvv_bp #(item_type) | Список портов приёмников    |

Класс содержит следующие функции/задачи:
| Имя       | Описание                              |
| --------- | ------------------------------------- |
| new       | Конструктор класса                    |
| write     | Функция для односторонней пересылки   |
| connect   | Задача для соединения портов          |

## Использование класса

При объявления экземпляра класса необходимо указать тип переменной подлежащей обмену и имя:
```verilog
    // Создание экземпляра класса с системным типом переменной
    dvv_aep     #(int)      item_aep_0;
    // Создание экземпляра класса с пользовательским типом переменной
    dvv_aep     #(user_t)   item_aep_1;
```

Создание экземпляра класса производится вызовом конструктора:
```verilog
    item_aep_0 = new("item_aep_0");
    item_aep_1 = new("item_aep_1");
```

Для подключения экземпляра класса к экземпляру (analysis port) необходимо вызвать функцию connect базового класса dvv_bp, при этом типы переменных для обмена должны совпадать. Также к одному экземпляру класса dvv_aep можно подключать несколько dvv_ap:
```verilog
    // Подключение к item_aep_0 нескольких analysis port
    item_aep_0.connect(item_ap_00);
    item_aep_0.connect(item_ap_01);
    item_aep_0.connect(item_ap_02);

    item_aep_1.connect(item_ap_1);
```

Функция write служит для односторонней пересылки данных:
```verilog
    int counter;
    counter = 8'h55;
    item_aep_0.write(counter);
```

Необходимо отметить, что передаваемая переменная имеет модификатор ref, что означает возможность передачи только переменных, передача констант недоступна.

Данный класс чаще всего используется в классах наследуемых от dvv_mon, то есть мониторов интерфейсов, транзакций, сигналов.