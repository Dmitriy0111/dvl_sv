# dvv_domain (domain class)
Данный файл содержит описание класса домена.

Заголовок:
```Verilog
class dvv_domain extends dvv_bc;
```

## Поля и функции/задачи класса  

Данный класс наследует поля и функции/задачи из базового класса компонентов [dvv_bc.md](dvv_bc.md).

### Поля:

| Имя       | Тип           | Описание                      |
| --------- | ------------- | ----------------------------- |
| phase_q   | dvv_phase [$] | Очередь фаз выполнения теста  |

#### Значение некоторых полей:

| Поле      | Значение      |
| --------- | ------------- |
| type_name | "dvv_domain"  |

### Функции/Задачи:
Класс содержит следующие функции/задачи:
| Имя           | Описание                                      |
| ------------- | --------------------------------------------- |
| new           | Конструктор класса                            |
| add_phase     | Задача для добавления фазы                    |
| add_phases    | Задача для добавления стандартного набора фаз |
| exec          | Задача для исполнения фаз                     |

### Описание функций/задач:

#### new
Конструктор класса для создания экземпляра dvv_domain.

Заголовок:
```Verilog
function new(string name = "", dvv_bc parent = null);
```

Аргументы:
| Имя       | Тип       | Описание                  |
| --------- | --------- | ------------------------- |
| name      | string    | Имя компонента            |
| parent    | dvv_bc    | Экземпляр класса родителя |

#### add_phase
Задача для добавления фазы.

Заголовок:
```Verilog
task add_phase(dvv_phase phase);
```

Аргументы:
| Имя   | Тип       | Описание              |
| ----- | --------- | --------------------- |
| phase | dvv_phase | Фаза для добавления   |

#### add_phases
Задача для добавления стандартного набора фаз.

Заголовок:
```Verilog
task add_phases();
```

#### exec
Задача для исполнения фаз.

Заголовок:
```Verilog
task exec();
```